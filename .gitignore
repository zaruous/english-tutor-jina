# Logs and runtime files
*.log
.DS_Store
.idea/
.vscode/

# Dependencies (none required for prototype, but for future)
node_modules/
package-lock.json
yarn.lock

# Build outputs
dist/
build/
.next/
.cache/

# Environment — 실제 접속정보. .env.example 만 추적한다
.env*
!.env.example

# Ollama models (massive; should not be committed)
*.gguf
models/

# User data (when backend is added)
data/
uploads/
*.db
*.sqlite

# CLI provider 전용 작업 디렉터리 (Phase 3)
.jina-agent-cwd/

# Playwright MCP 브라우저 캡처 (리뷰용 임시)
.playwright-mcp/

# 발음 평가 사이드카 (lib/pronounce)
lib/pronounce/.venv/
lib/pronounce/.sidecar.pid
lib/pronounce/sidecar.log
__pycache__/
# 사내 프록시(SSL 검사) 환경 전용 — Dockerfile 이 있으면 신뢰 저장소에 넣는다. 커밋 금지.
lib/pronounce/corp-ca.crt

# Claude Code 로컬 워크트리 (세션별 임시)
.claude/worktrees/

# e2e 스크린샷 등 실행마다 덮어써지는 산출물 (라운드 폴더에는 골라 복사)
docs/reviews/_artifacts/
