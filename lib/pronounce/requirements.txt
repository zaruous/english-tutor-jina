# OpenPronounce 사이드카 의존성 (플랜 10)
#
# torch 는 여기 두지 않는다 — 그냥 설치하면 CUDA 휠(2GB+)을 받는다.
# install-python.ps1 과 Dockerfile 이 CPU 휠을 먼저 설치한 뒤 이 파일을 처리한다:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# tts-piper: compare_audio_with_text 는 목표 문장을 TTS 로 합성해 DTW 비교를 한다.
# 기본 백엔드 gtts 는 그 문장을 Google 로 보내고 인터넷을 요구한다 — piper 는 완전 오프라인이다
# (음성 ~60MB 최초 1회 다운로드, 합성 결과는 문장별로 캐시된다).
openpronounce[tts-piper]==0.3.0

fastapi>=0.100
uvicorn[standard]>=0.23
python-multipart>=0.0.6
